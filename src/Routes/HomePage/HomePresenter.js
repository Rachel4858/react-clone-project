import React from "react";
import PropTypes from "prop-types";
import styled from "styled-components";

const Container = styled.div`
  padding:10px 50px;
  font-size:15px;
`;

const HomePresenter = () => (
  <Container>home 프레젠터 준비중 입니다..</Container>
);

export default HomePresenter